This package allows recording joint trajectories sent to an action server alongside their counterparts executed on the robot for comparison.
## Requirements
With the node, launch `cca_<robot>_description` params (as you would for a `CcaRos` node), along with `output_dir` param indicating the directory to save the recorded data.
